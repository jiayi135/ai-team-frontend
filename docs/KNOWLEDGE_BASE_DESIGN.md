# 知识库建议系统设计文档

## 设计目标

基于 Manus 的知识库建议功能，为 AI Team Frontend 项目设计并实现一个智能知识库系统，使 AI 代理能够：

1. **主动提供建议** - 根据用户输入和上下文，主动推荐相关知识
2. **确保逻辑通畅** - 通过知识库验证和补充，确保 AI 响应的准确性
3. **持续学习** - 从对话和任务执行中积累知识
4. **符合 P.R.O.M.P.T. 框架** - 与现有架构无缝集成

## 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  聊天界面     │  │  知识库管理   │  │  建议面板     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        API 路由层                             │
│  /api/chat/message  /api/knowledge/*  /api/suggestions      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        服务层                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ChatService  │  │KnowledgeBase │  │SuggestionEng │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  向量数据库   │  │  关系数据库   │  │  缓存层       │      │
│  │  (ChromaDB)  │  │  (SQLite)    │  │  (Memory)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 核心组件

#### 1. Knowledge Base Service（知识库服务）

**职责**：
- 管理知识条目的 CRUD 操作
- 向量化知识内容
- 语义搜索和检索
- 知识分类和标签管理

**数据结构**：
```typescript
interface KnowledgeEntry {
  id: string;
  title: string;
  content: string;
  category: string;
  tags: string[];
  source: 'manual' | 'learned' | 'imported';
  confidence: number;  // 0-1
  usageCount: number;
  lastUsed?: Date;
  createdAt: Date;
  updatedAt: Date;
  embedding?: number[];  // 向量表示
}
```

#### 2. Suggestion Engine（建议引擎）

**职责**：
- 分析用户输入
- 检索相关知识
- 生成智能建议
- 排序和过滤建议

**建议类型**：
```typescript
interface Suggestion {
  id: string;
  type: 'knowledge' | 'skill' | 'task' | 'resource';
  title: string;
  description: string;
  relevance: number;  // 0-1
  source: string;
  action?: {
    type: 'apply' | 'view' | 'execute';
    payload: any;
  };
}
```

#### 3. Learning Module（学习模块）

**职责**：
- 从对话中提取知识
- 从任务执行中总结经验
- 自动更新知识库
- 知识质量评估

**学习来源**：
- 用户对话
- 任务执行结果
- 工具调用记录
- 文档和代码

## 知识库分类

### 按来源分类

1. **Manual（手动添加）**
   - 团队成员手动录入
   - 文档导入
   - 最高可信度

2. **Learned（自动学习）**
   - 从对话中提取
   - 从任务中总结
   - 需要验证

3. **Imported（外部导入）**
   - HF 文档
   - GitHub README
   - 技术文档

### 按类型分类

1. **Framework Knowledge（框架知识）**
   - P.R.O.M.P.T. 原则
   - 架构设计
   - 最佳实践

2. **Technical Knowledge（技术知识）**
   - API 使用方法
   - 代码示例
   - 配置说明

3. **Domain Knowledge（领域知识）**
   - AI 模型知识
   - 数据集信息
   - 工具使用

4. **Operational Knowledge（操作知识）**
   - 常见问题解决
   - 故障排查
   - 优化建议

## 建议生成流程

### 流程图

```
用户输入
    ↓
意图识别
    ↓
上下文分析 ←──────┐
    ↓              │
语义搜索           │
    ↓              │
相关性评分         │
    ↓              │
建议生成           │
    ↓              │
排序过滤           │
    ↓              │
返回建议           │
    ↓              │
用户反馈 ──────────┘
```

### 详细步骤

#### 1. 意图识别

分析用户输入，识别意图类型：
- 问题咨询
- 任务请求
- 功能查询
- 故障报告

#### 2. 上下文分析

收集相关上下文：
- 当前对话历史
- 用户角色和权限
- 当前任务状态
- 系统状态

#### 3. 语义搜索

使用向量相似度搜索：
```typescript
async searchKnowledge(query: string, limit: number): Promise<KnowledgeEntry[]> {
  const queryEmbedding = await this.embedText(query);
  const results = await this.vectorDB.search(queryEmbedding, limit);
  return results;
}
```

#### 4. 相关性评分

综合多个因素计算相关性：
- 语义相似度（0.4 权重）
- 使用频率（0.2 权重）
- 时效性（0.2 权重）
- 可信度（0.2 权重）

#### 5. 建议生成

根据知识生成可操作的建议：
```typescript
interface ActionableSuggestion {
  knowledge: KnowledgeEntry;
  action: {
    type: 'apply' | 'view' | 'execute' | 'learn_more';
    label: string;
    handler: () => Promise<void>;
  };
}
```

#### 6. 排序过滤

- 按相关性降序排序
- 过滤低相关性建议（< 0.3）
- 去重相似建议
- 限制返回数量（默认 5 个）

## 与 P.R.O.M.P.T. 框架的集成

### Purpose（目标定义）

知识库帮助明确任务目标：
- 提供相关背景知识
- 澄清模糊需求
- 建议最佳实践

### Role（角色与边界）

知识按角色分类：
- Architect 知识
- Developer 知识
- Tester 知识
- 通用知识

### Operation（操作与结构）

结构化的知识表示：
- 标准化格式
- 可执行的建议
- 清晰的操作步骤

### Media（上下文与探索）

深度挖掘相关知识：
- 关联知识推荐
- 相关资源链接
- 扩展阅读建议

### Planned（迭代规划）

预测性知识推荐：
- 下一步可能需要的知识
- 潜在问题的预警
- 优化建议

### Tracing（追溯与验证）

知识来源追溯：
- 记录知识来源
- 验证知识准确性
- 追踪使用效果

## 数据存储设计

### 向量数据库（ChromaDB）

存储知识的向量表示，用于语义搜索：

```python
collection = chroma_client.create_collection(
    name="knowledge_base",
    metadata={"description": "AI Team knowledge base"}
)

collection.add(
    documents=[entry.content],
    metadatas=[{
        "id": entry.id,
        "title": entry.title,
        "category": entry.category,
        "tags": ",".join(entry.tags),
        "confidence": entry.confidence
    }],
    ids=[entry.id]
)
```

### 关系数据库（SQLite）

存储知识的结构化数据：

```sql
CREATE TABLE knowledge_entries (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    category TEXT NOT NULL,
    tags TEXT,  -- JSON array
    source TEXT NOT NULL,
    confidence REAL DEFAULT 1.0,
    usage_count INTEGER DEFAULT 0,
    last_used DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE knowledge_relations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    from_id TEXT NOT NULL,
    to_id TEXT NOT NULL,
    relation_type TEXT NOT NULL,  -- 'related', 'prerequisite', 'alternative'
    strength REAL DEFAULT 0.5,
    FOREIGN KEY (from_id) REFERENCES knowledge_entries(id),
    FOREIGN KEY (to_id) REFERENCES knowledge_entries(id)
);

CREATE TABLE suggestion_feedback (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    suggestion_id TEXT NOT NULL,
    knowledge_id TEXT NOT NULL,
    user_action TEXT NOT NULL,  -- 'accepted', 'rejected', 'ignored'
    context TEXT,  -- JSON
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (knowledge_id) REFERENCES knowledge_entries(id)
);
```

### 缓存层（Memory）

缓存热门知识和最近使用的建议：

```typescript
class KnowledgeCache {
  private cache: Map<string, CacheEntry>;
  private maxSize: number = 100;
  private ttl: number = 3600000;  // 1 hour

  get(key: string): KnowledgeEntry | null;
  set(key: string, value: KnowledgeEntry): void;
  invalidate(key: string): void;
  clear(): void;
}
```

## API 设计

### 知识库管理 API

```typescript
// 获取知识列表
GET /api/knowledge
Query: { category?, tags?, search?, limit?, offset? }
Response: { knowledge: KnowledgeEntry[], total: number }

// 获取单个知识
GET /api/knowledge/:id
Response: { knowledge: KnowledgeEntry }

// 创建知识
POST /api/knowledge
Body: { title, content, category, tags }
Response: { knowledge: KnowledgeEntry }

// 更新知识
PUT /api/knowledge/:id
Body: { title?, content?, category?, tags? }
Response: { knowledge: KnowledgeEntry }

// 删除知识
DELETE /api/knowledge/:id
Response: { success: boolean }

// 搜索知识
POST /api/knowledge/search
Body: { query, limit?, filters? }
Response: { results: KnowledgeEntry[], relevance: number[] }
```

### 建议 API

```typescript
// 获取建议
POST /api/suggestions
Body: { 
  query: string,
  context?: {
    conversationId?: string,
    taskId?: string,
    role?: string
  },
  limit?: number
}
Response: { 
  suggestions: Suggestion[],
  metadata: {
    totalFound: number,
    searchTime: number
  }
}

// 反馈建议
POST /api/suggestions/:id/feedback
Body: { 
  action: 'accepted' | 'rejected' | 'ignored',
  comment?: string
}
Response: { success: boolean }
```

### 学习 API

```typescript
// 从对话学习
POST /api/knowledge/learn/conversation
Body: { 
  conversationId: string,
  messages: Message[]
}
Response: { 
  learned: KnowledgeEntry[],
  confidence: number[]
}

// 从任务学习
POST /api/knowledge/learn/task
Body: { 
  taskId: string,
  result: any,
  success: boolean
}
Response: { 
  learned: KnowledgeEntry[],
  insights: string[]
}
```

## 前端界面设计

### 1. 聊天界面中的建议面板

```
┌─────────────────────────────────────────┐
│  Agent 对话                              │
├─────────────────────────────────────────┤
│  用户: 如何使用 Hugging Face 模型？      │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 💡 相关建议                         │ │
│  ├────────────────────────────────────┤ │
│  │ 📚 Hugging Face 集成指南            │ │
│  │    查看完整的集成文档和示例         │ │
│  │    [查看] [应用]                    │ │
│  ├────────────────────────────────────┤ │
│  │ 🔧 推荐模型列表                     │ │
│  │    8 个推荐的 HF 模型               │ │
│  │    [查看] [搜索]                    │ │
│  └────────────────────────────────────┘ │
│                                          │
│  AI: 我为您找到了相关的知识和资源...    │
└─────────────────────────────────────────┘
```

### 2. 知识库管理页面

```
┌─────────────────────────────────────────┐
│  知识库管理                              │
├─────────────────────────────────────────┤
│  [搜索框] [分类筛选] [+ 添加知识]       │
├─────────────────────────────────────────┤
│  ┌────────────────────────────────────┐ │
│  │ 📚 Framework Knowledge (12)        │ │
│  │ ├─ P.R.O.M.P.T. 六大支柱           │ │
│  │ ├─ 角色定义和边界                  │ │
│  │ └─ 协商机制                        │ │
│  └────────────────────────────────────┘ │
│  ┌────────────────────────────────────┐ │
│  │ 🔧 Technical Knowledge (25)        │ │
│  │ ├─ Hugging Face API 使用           │ │
│  │ ├─ MCP 服务器配置                  │ │
│  │ └─ 代码生成最佳实践                │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 3. 知识详情页面

```
┌─────────────────────────────────────────┐
│  ← 返回  Hugging Face API 使用          │
├─────────────────────────────────────────┤
│  分类: Technical Knowledge              │
│  标签: [API] [HF] [Integration]         │
│  来源: Manual  可信度: ★★★★★            │
│  使用次数: 45  最后使用: 2 小时前       │
├─────────────────────────────────────────┤
│  内容:                                   │
│                                          │
│  要使用 Hugging Face Inference API:     │
│                                          │
│  1. 安装依赖...                         │
│  2. 配置 API Key...                     │
│  3. 调用模型...                         │
│                                          │
│  [代码示例]                             │
│  ```typescript                          │
│  const client = new InferenceClient()   │
│  ```                                    │
├─────────────────────────────────────────┤
│  相关知识:                              │
│  • HF 模型推荐列表                      │
│  • API 成本优化                         │
│  • 错误处理最佳实践                     │
├─────────────────────────────────────────┤
│  [编辑] [删除] [分享]                   │
└─────────────────────────────────────────┘
```

## 实现优先级

### P0 - 核心功能（第 1-2 周）

- ✅ 知识库基础数据结构
- ✅ CRUD API 实现
- ✅ 简单的关键词搜索
- ✅ 聊天界面中的建议显示
- ✅ 手动添加知识功能

### P1 - 智能功能（第 3-4 周）

- ⏳ 向量数据库集成（ChromaDB）
- ⏳ 语义搜索实现
- ⏳ 相关性评分算法
- ⏳ 建议排序和过滤
- ⏳ 知识库管理页面

### P2 - 高级功能（第 5-8 周）

- 📋 自动学习模块
- 📋 知识关系图谱
- 📋 多语言支持
- 📋 知识版本控制
- 📋 协作编辑

## 技术选型

### 后端

- **向量数据库**: ChromaDB（轻量级，易于集成）
- **关系数据库**: SQLite（已有）
- **向量化**: OpenAI Embeddings API 或 Sentence Transformers
- **缓存**: 内存缓存（Map）

### 前端

- **UI 组件**: 现有的 shadcn/ui + Ant Design
- **状态管理**: React Hooks + Context
- **搜索**: 实时搜索 + 防抖
- **编辑器**: Markdown 编辑器（react-markdown）

## 性能优化

### 1. 缓存策略

- 热门知识缓存
- 搜索结果缓存
- 向量缓存

### 2. 批量操作

- 批量向量化
- 批量插入
- 批量更新

### 3. 异步处理

- 异步学习
- 异步索引
- 异步更新

### 4. 分页加载

- 知识列表分页
- 搜索结果分页
- 无限滚动

## 安全考虑

### 1. 权限控制

- 知识的创建、编辑、删除权限
- 敏感知识的访问控制
- 审计日志

### 2. 内容审核

- 自动学习的知识需要审核
- 敏感信息过滤
- 质量评估

### 3. 数据隔离

- 用户数据隔离
- 团队数据隔离
- 多租户支持（未来）

## 测试策略

### 1. 单元测试

- 知识库 CRUD 操作
- 搜索算法
- 相关性评分

### 2. 集成测试

- API 端到端测试
- 数据库集成测试
- 向量数据库测试

### 3. 性能测试

- 搜索性能
- 大量数据下的性能
- 并发测试

### 4. 用户测试

- 建议准确性测试
- 用户体验测试
- A/B 测试

## 监控和分析

### 1. 使用统计

- 知识使用频率
- 建议接受率
- 搜索热词

### 2. 质量指标

- 建议相关性
- 用户满意度
- 知识覆盖率

### 3. 性能指标

- 搜索响应时间
- API 响应时间
- 数据库查询时间

## 总结

本设计文档定义了一个完整的知识库建议系统，它将：

1. **提升 AI 代理的智能性** - 通过知识库提供准确、相关的建议
2. **确保逻辑通畅** - 基于 P.R.O.M.P.T. 框架，保持系统一致性
3. **持续进化** - 通过学习模块不断积累和优化知识
4. **易于使用** - 直观的界面和智能的建议

下一步将按照优先级逐步实现各个功能模块。

---

**文档版本**: 1.0  
**创建日期**: 2026-02-07  
**作者**: Manus AI Agent
