# Vercel 网页部署指南

## 🚀 5 分钟完成永久部署

本指南将帮助您通过 Vercel 网页界面将项目部署为永久可访问的网站。

---

## 📋 前置条件

- ✅ GitHub 账号：jiayi135
- ✅ 项目仓库：https://github.com/jiayi135/ai-team-frontend
- ✅ Vercel 账号：huzhitao117@outlook.com（密码：Lobe123456!）

---

## 🎯 部署步骤

### 步骤 1: 登录 Vercel

1. 访问：https://vercel.com/login
2. 选择 "Continue with Email"
3. 输入邮箱：`huzhitao117@outlook.com`
4. 输入密码：`Lobe123456!`
5. 或者选择 "Continue with GitHub" 使用 GitHub 登录

### 步骤 2: 导入项目

1. 登录后，点击右上角的 **"Add New..."** 按钮
2. 选择 **"Project"**
3. 在 "Import Git Repository" 页面：
   - 如果看到仓库列表，直接选择 `jiayi135/ai-team-frontend`
   - 如果没有看到，点击 "Import Git Repository"，输入：
     ```
     https://github.com/jiayi135/ai-team-frontend
     ```

### 步骤 3: 配置项目

在 "Configure Project" 页面：

**Framework Preset**: 选择 `Other`

**Build and Output Settings**:
- Build Command: `pnpm run build`
- Output Directory: `dist/public`
- Install Command: `pnpm install`

**Root Directory**: `.` (保持默认)

**Environment Variables** (可选):
如果需要添加环境变量，点击 "Add" 添加：
```
GEMINI_API_KEY=your_gemini_api_key_here
HF_TOKEN=your_huggingface_token_here
```

### 步骤 4: 部署

1. 点击 **"Deploy"** 按钮
2. 等待 2-3 分钟，Vercel 会自动：
   - 克隆代码
   - 安装依赖
   - 构建项目
   - 部署到全球 CDN

### 步骤 5: 获取网址

部署成功后，您会看到：
- ✅ 成功页面
- 🌐 永久访问地址（类似：`https://ai-team-frontend-xxx.vercel.app`）
- 🎉 项目已上线！

---

## 🔧 高级配置

### 自定义域名

1. 进入项目设置（Settings）
2. 选择 "Domains"
3. 添加您的自定义域名
4. 按照提示配置 DNS

### 环境变量管理

1. 进入项目设置（Settings）
2. 选择 "Environment Variables"
3. 添加/修改/删除环境变量
4. 重新部署生效

### 自动部署

Vercel 已自动配置：
- ✅ 每次 push 到 `master` 分支自动部署
- ✅ 每次 PR 创建预览部署
- ✅ 部署状态通知

---

## ⚠️ 注意事项

### 关于后端服务

**重要**: Vercel 主要用于静态网站和 Serverless Functions。

由于您的项目包含完整的 Express 后端服务器，有两个方案：

#### 方案 A: 仅部署前端（推荐）

1. 前端部署到 Vercel
2. 后端部署到 Railway/Render
3. 前端通过 API 调用后端

**优点**: 
- ✅ 前端快速、稳定
- ✅ 后端独立运行
- ✅ 易于扩展

#### 方案 B: 全栈部署到 Vercel

Vercel 支持 Serverless Functions，但需要修改后端代码：
- 将 Express 路由转换为 Serverless Functions
- 每个 API 路由一个文件
- 有执行时间限制（10 秒）

**建议**: 使用方案 A，前后端分离部署

---

## 🚀 后端部署（Railway）

### 步骤 1: 登录 Railway

1. 访问：https://railway.app
2. 使用 GitHub 登录

### 步骤 2: 创建项目

1. 点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 选择 `jiayi135/ai-team-frontend`
4. Railway 会自动检测并部署

### 步骤 3: 配置

1. 添加环境变量（如需要）
2. 配置启动命令：`node dist/index.js`
3. 等待部署完成

### 步骤 4: 获取后端 URL

部署成功后，Railway 会提供一个 URL：
```
https://ai-team-frontend-production.up.railway.app
```

### 步骤 5: 连接前后端

在 Vercel 的环境变量中添加：
```
VITE_API_URL=https://ai-team-frontend-production.up.railway.app
```

重新部署前端即可。

---

## 📊 部署后检查清单

- [ ] 前端可以访问
- [ ] 页面加载正常
- [ ] 样式显示正确
- [ ] 后端 API 可以调用（如已部署）
- [ ] 功能测试通过
- [ ] 自定义域名配置（可选）

---

## 🎉 完成！

恭喜！您的项目现在已经是一个永久可访问的网站了！

**前端地址**: `https://ai-team-frontend-xxx.vercel.app`  
**后端地址**: `https://ai-team-frontend-production.up.railway.app` (如已部署)

---

## 💡 常见问题

### Q: 部署失败怎么办？

A: 查看构建日志，常见问题：
- 依赖安装失败 → 检查 package.json
- 构建命令错误 → 检查 build 脚本
- 内存不足 → 优化构建过程

### Q: 如何更新网站？

A: 只需 push 代码到 GitHub：
```bash
git add .
git commit -m "update"
git push
```
Vercel 会自动重新部署。

### Q: 如何回滚到之前的版本？

A: 在 Vercel 控制台：
1. 进入 "Deployments"
2. 找到之前的部署
3. 点击 "Promote to Production"

### Q: 部署需要付费吗？

A: Vercel 和 Railway 都有免费套餐：
- Vercel: 100GB 带宽/月
- Railway: $5 免费额度/月

对于个人项目完全够用。

---

## 📚 相关资源

- Vercel 文档：https://vercel.com/docs
- Railway 文档：https://docs.railway.app
- 项目仓库：https://github.com/jiayi135/ai-team-frontend

---

**祝部署顺利！** 🚀✨
